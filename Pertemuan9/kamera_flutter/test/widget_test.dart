// This is a basic Flutter widget test.
//
// Widget test ini dinonaktifkan karena app sekarang menggunakan kamera
// yang membutuhkan perangkat fisik atau emulator untuk diuji.

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Kamera app test placeholder', (WidgetTester tester) async {
    // Test ini dinonaktifkan karena memerlukan kamera hardware.
    // Jalankan langsung di device/emulator menggunakan `flutter run`.
    expect(true, isTrue);
  });
}
